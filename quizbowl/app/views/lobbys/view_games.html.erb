<h1 class = "text-center">Welcome To The Lobby!</h1>

<h4 class = "text-center"> Please browse for a game, or search based on location, name, or player.</h4>
<h5 class = "text-center"> When you're ready, choose a game and join...It's that simple.</h5>

<div class="row">
  <div class="large-1 columns myinline"></div>
  <div class="large-11 columns myinline">
  	<!-- <button type="submit" class="button [tiny small large]">Submit</button> -->
  	<input type="submit"value="Find" style="float: left;" class="button [tiny small large]"/>
	<div style="overflow: hidden; padding-right: .5em;">
   <input type="text" style="width: 717px; height: 45px" placeholder=" location, name, or player"  />
	</div>​
  	<!-- <input value = "Search" style="width: 600px" type="text" placeholder="large-4.columns" /> -->
  </div>
</div>

<div class="row">
  <div class="large-1 columns"></div>
  <div class="large-11 columns">
  	<div style="overflow: auto;">
		<table style="width: 800px" cellpadding="0" cellspacing="0">
			<tr>
  			<td><b>Host</b></td>
  			<td style="text-indent: 215px"><b>Description</b></td>
  			<td><b>Location</b></td>
			</tr>
		</table>
	</div>
<div style="overflow: auto;height: 400px; width: 820px;">
  <table style="width: 800px;" cellpadding="0" cellspacing="0">
  <% @games.each do |game| %>
  	<tr>
	<td><%= link_to game.name, waiting_area_path(game.id) %></td>
	<td><%= game.description %></td>
	<td>location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

	<% @games.each do |game| %>
  	<tr>
	<td><%= game.name %></td>
	<td><%= game.description %></td>
	<td>Location</td>
	</tr>
	<% end %>

  <!-- <tr>
    <td>Value 1</td>
    <td>Value 2</td>
    <td>Value 3</td>
  </tr>
  -->
  </table>
</div>
</div>
</div>



