
  <%= link_to "Edit Your Account", edit_user_path(@user.id), class: 'pull-right btn btn-large btn-info' %>
  <%= link_to "Delete Your Account", user_path(@user.id), method: 'delete', class: 'pull-right btn btn-large btn-danger' %>

  <h1><%= @user.username %></h1>


<div class="row">
  <div class="col-md-9">
    <p class="lead">
      <strong>Name:</strong>
      <%= @user.first %> <%= @user.last %>
    </p>
    <p class="lead"><strong>Age: </strong><%= @user.age %></p>
  </div>
</div>